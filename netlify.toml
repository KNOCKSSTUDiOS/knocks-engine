[build]
  publish = "."
  functions = "functions"
  command = "npm install && echo 'Build complete'"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[plugins]]
  package = "netlify-plugin-cypress"
  [plugins.inputs]
    skip = true
